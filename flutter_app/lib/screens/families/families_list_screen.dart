import 'package:flutter/material.dart';

/// Families List Screen (Placeholder)
class FamiliesListScreen extends StatelessWidget {
  const FamiliesListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Familien'),
      ),
      body: const Center(
        child: Text('Familien-Verwaltung\n(TODO: Implementierung)'),
      ),
    );
  }
}
